name: test

on:
  push:
    branches: 
      - '*'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          path: "heighliner-action"

      - name: Test heighliner action locally
        uses: ./heighliner-action
